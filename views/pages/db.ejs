<!DOCTYPE html>
<html>
<body>

<h2>Database Results</h2>

<ul>
    <% results.forEach(function(r) { %>
        <li><%= r.id %>  -  <%= r.firstname %>  -  <%= r.lastname %>  -  <%= r.email %>  --  <%= r.phonenumber %></li>
    <% }); %>
</ul>

</body>
</html>
